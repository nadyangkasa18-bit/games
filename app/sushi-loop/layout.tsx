import "./sushi-loop.css";

export const metadata = {
  title: "Sushi Loop",
  description: "A live two-player card drafting game.",
};

export default function SushiLoopLayout({ children }: { children: React.ReactNode }) {
  return children;
}
